const Barcode = require('../models/AutoGeneratedBarcodes');

module.exports.findLastBarcode = async () => {
	const lastBarcode = await Barcode.findOne().sort({ createdAt: -1 });
	return lastBarcode?.barcodeNumber;
}